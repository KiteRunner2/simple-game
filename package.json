{
  "scripts": {
    "ts-check": "tsc --noEmit",
    "build": "esbuild --bundle src/index.ts --outfile=compiled/script.js --minify",
    "deploy": "tsc --noEmit && esbuild --bundle src/index.ts --outfile=compiled/script.js --minify"
  },
  "dependencies": {
    "esbuild": "^0.14.47",
    "typescript": "^4.7.4"
  }
}
